{
  "$schema": "https://railway.app/schema.json",
  "name": "ai-api-dashboard",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "AUTH_TRUST_HOST=true AUTH_ORIGIN=https://key-dash-production.up.railway.app npx prisma generate && npx prisma db push && npm start",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  },
  "env": {
  }
}
